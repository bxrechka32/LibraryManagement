<Window x:Class="LibraryManagement.Views.BookDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" 
        Height="400" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Название -->
        <TextBlock Grid.Row="0" Grid.Column="0" 
                   Text="Название:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <TextBox Grid.Row="0" Grid.Column="1" 
                 Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" Padding="4" />

        <!-- Автор -->
        <TextBlock Grid.Row="1" Grid.Column="0" 
                   Text="Автор:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <ComboBox Grid.Row="1" Grid.Column="1" 
                  ItemsSource="{Binding Authors}"
                  SelectedItem="{Binding SelectedAuthor}"
                  DisplayMemberPath="FullName"
                  Margin="0,0,0,10" />

        <!-- Жанр -->
        <TextBlock Grid.Row="2" Grid.Column="0" 
                   Text="Жанр:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <ComboBox Grid.Row="2" Grid.Column="1" 
                  ItemsSource="{Binding Genres}"
                  SelectedItem="{Binding SelectedGenre}"
                  DisplayMemberPath="Name"
                  Margin="0,0,0,10" />

        <!-- Год издания -->
        <TextBlock Grid.Row="3" Grid.Column="0" 
                   Text="Год издания:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <TextBox Grid.Row="3" Grid.Column="1" 
                 Text="{Binding PublishYear, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" Padding="4" />

        <!-- ISBN -->
        <TextBlock Grid.Row="4" Grid.Column="0" 
                   Text="ISBN:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <TextBox Grid.Row="4" Grid.Column="1" 
                 Text="{Binding ISBN, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" Padding="4" />

        <!-- Количество -->
        <TextBlock Grid.Row="5" Grid.Column="0" 
                   Text="Количество:" VerticalAlignment="Center" 
                   Margin="0,0,10,10" FontWeight="SemiBold" />
        <TextBox Grid.Row="5" Grid.Column="1" 
                 Text="{Binding QuantityInStock, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" Padding="4" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="Сохранить" 
                    Command="{Binding SaveCommand}"
                    Padding="20,8" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White"
                    FontWeight="SemiBold" BorderThickness="0" />

            <Button Content="Отмена" 
                    Command="{Binding CancelCommand}"
                    Padding="20,8"
                    Background="#9E9E9E" Foreground="White"
                    FontWeight="SemiBold" BorderThickness="0" />
        </StackPanel>
    </Grid>
</Window>
